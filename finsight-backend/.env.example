# Copy to .env and replace the placeholder locally. Do not add quotes.
DB_PASSWORD=replace_with_your_existing_postgres_password
# Set a private random signing key of at least 32 UTF-8 bytes (no quotes).
JWT_SECRET=
